# Default Configuration
# 
# This file contains default settings for folder-mcp.
# Users can override these settings in config.yaml

# User Interface Theme Configuration
theme: "auto"  # Options: light, dark, auto

# Folder defaults
folders:
  defaults:
    embeddings:
      model: "{{DEFAULT_MODEL}}"
      batchSize: 32

# Development settings
development:
  enabled: false

# Memory monitoring configuration
memoryMonitor:
  enabled: false

# ONNX Performance Optimization Settings (testing configuration system)
onnx:
  workerPoolSize: 2        # Testing showed optimal 2
  threadsPerWorker: 2      # Testing showed optimal 2
  batchSize: 1             # Testing showed optimal 1
  maxConcurrentFiles: 4    # Testing showed optimal 4

# Logging configuration
logging:
  level: info
  componentLevels: {}
  transports:
    console:
      enabled: true
      level: info
      format: text
    file:
      enabled: false
      level: debug
      format: json
      filename: ~/.folder-mcp/logs/daemon.log
  rotation:
    enabled: false
    maxSize: 50MB
    maxFiles: 5
    rotateOnStartup: false
  performance:
    enableMonitoring: false
    maxLatencyMs: 100
    alertThreshold: 1000
    sampleRate: 0.1
  enableMetrics: false